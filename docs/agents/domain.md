# 领域文档

## 开始探索前

- 阅读仓库根目录的 `CONTEXT.md`。
- 如果 `docs/adr/` 存在，阅读与当前工作相关的 ADR。

文件或目录不存在时，直接继续，不要主动创建；只有形成需要长期保留的技术决策时才新增 ADR。

## 文档布局

本仓库使用单一上下文：

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Issue、方案、测试和文档中的领域术语应以 `CONTEXT.md` 为准；如与既有 ADR 冲突，必须明确指出。
